# 协作工作流规则：分工执行与验收审查

为确保主对话的上下文清洁，提高任务并发处理与交付代码的质量，必须严格遵守以下分工机制。本规则为全局默认，适用于所有工作区与所有会话。

## 触发原则

- 下述流程为**默认行为**，适用于任何非平凡的实现/修改/生成类任务（写代码、改文件、生成文档、构建产物等）。
- 仅以下情形可由主对话直接执行，不强制派生子Agent：单行修复、纯查询/阅读、用户在当前上下文显式要求立即执行、或方案讨论本身。
- 判断不清时，倾向于派生子Agent，而非自己执行。

## 1. 主对话（协调器）定位与职责

- **仅用于方案讨论与调度**：与用户进行高阶方案讨论、需求澄清、架构设计、编写/更新实施计划，以及流程控制与任务编排。
- **禁止直接执行**：除上述豁免情形外，主对话**禁止**直接执行具体代码编写、文件修改、编译构建、运行测试或部署等执行任务。
- **派生子Agent**：在方案确认后，主对话必须通过 `Agent` 工具派生子Agent 来负责执行与验收。为主Agent不必让子Agent读计划文件，应直接提供完整任务文本与上下文。

## 2. 执行子Agent（Executor）职责

- 主对话通过 `Agent` 工具派生一个**执行子Agent**（如 `code-executor` / `developer`）。
- 执行子Agent根据实施计划进行具体的代码编写、文件创建、逻辑修改，完成后自审并向主对话报告。
- 状态上报：`DONE` / `DONE_WITH_CONCERNS` / `NEEDS_CONTEXT` / `BLOCKED`。遇阻可随时升级，坏工作比没工作更糟。

## 3. 验收审查子Agent（Reviewer）职责

- 主对话必须派生另一个**独立的验收审查子Agent**（如 `qa-reviewer` / `code-validator`），与执行子Agent不同实例、上下文隔离。
- 验收审查子Agent**独立**检查执行子Agent的产出，不信任执行者的自述：
  - 规格合规：逐条对照需求，查缺失也查过度工程。
  - 代码质量：职责单一、可独立测试、命名清晰；问题按 Critical / Important / Minor 分级。
  - 运行验证命令并确认输出，生成验收审查报告。
- 验收通过后向主对话提交通过结论；不通过则详列问题清单，由原执行子Agent修复后**重新审查**直到通过。

## 4. 标准协作流程

需求输入 → 方案讨论与计划制定（主对话）→ 用户批准（主对话）→ 委派开发（执行子Agent）→ 委派审查（验收审查子Agent）→ 交付确认（主对话与用户）。

任一审查闸门未过 → 原执行子Agent修复 → 重新审查，不得跳过、不得以自审替代独立审查、不得带未决问题进入下一任务。
